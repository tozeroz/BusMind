# 服务端工程师 B 测试记录

## 测试命令

```bash
PYTHONPATH=. pytest -q backend/tests tests/integration
```

## 结果

```text
26 passed
```

## 覆盖内容

| 模块 | 测试内容 |
|---|---|
| 正式主应用 | 从 `backend.app.main:app` 调用 ETA 和数据更新接口，不再返回 404 |
| ETA | 正常预测、线路不匹配、参数校验、更新结果优先级 |
| 客载状态 | 正常预测、车辆线路校验、更新结果优先级、SEA/SDA/LSD 映射 |
| 数据更新 | 车辆状态 PATCH、ETA 更新、客载更新、过期时间和字段校验 |
| LTA | 未配置 Key 的统一错误、模拟 LTA 响应写入 ETA/Load/车辆状态 |
| 出行体验 | 客载、步行、换乘三项评分及权重校验 |
| 推荐 | 综合推荐、禁止换乘、起终点相同异常 |
| AI | DeepSeek 客户端注入、无 Key 本地兜底、请求字段校验 |
| 集成流程 | 更新状态 → ETA → 客载 → 推荐 → AI 解释 |

## 已知边界

1. 模拟状态和预测更新保存在当前后端进程内，重启后清空；
2. LTA Load 是等级，不是精确乘客人数，代码只为评分生成代表性 load rate；
3. 服务端 A 接入数据库后，应实现网关的 `update_vehicle_status`，但本交付不修改其 Repository；
4. 测试产生的 SQLAlchemy/Pydantic 弃用警告来自服务端 A 现有代码，不影响 26 项测试通过，本次未跨职责修改。
