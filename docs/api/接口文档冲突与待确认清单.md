# 接口文档冲突与待确认清单

> 核对日期：2026-07-12。优先级：运行时 OpenAPI > 后端代码 > 前端真实调用 > 历史文档/注释。

| 编号 | 冲突/待确认 | 代码与 OpenAPI事实 | 影响 | 建议 | 状态 |
|---|---|---|---|---|---|
| C-01 | 文档统计过期 | 运行时 OpenAPI 为 60 条路径、72 个操作，含 `/` 与 `/api/v1/` 两个健康检查 | 总文档旧称 62 操作 | 以自动生成清单定期核对 | 已在总文档增量更正 |
| C-02 | Base URL 不一致 | `request.js` 默认 `/api`；旧文档称 `/api/v1` | 可能请求到错误路径 | 明确 Vite proxy 或把默认值统一为 `/api/v1` | 待确认部署配置 |
| C-03 | `page_size` 与 `limit` | 当前运行时列表/搜索分页为 `page`,`limit` | 旧调用会被忽略或 422 | 文档和前端统一 `limit` | 文档已更正，代码待巡检 |
| C-04 | `trace_id` 与 `request_id` | 两套 ApiResponse/运行时 Schema 使用 `trace_id` | 日志关联字段读取失败 | 文档统一 `trace_id`，前端错误页展示它 | 待前端接入 |
| C-05 | `predicted_eta_minutes` 与 `eta_minutes` | ETA Schema 与推荐结果均为 `predicted_eta_minutes`；组件 Mock 用 `eta_minutes`/`eta` | 页面适配失败；术语误导 | 现行契约保留旧字段并适配；后续若改名需版本化/兼容字段 | 待确认 API 演进方案 |
| C-06 | `low_load` 与 `comfort` | OpenAPI `Preference` 仍是 `low_load`，无 `comfort`；代码未见映射 | 正式前端传 comfort 会 422 | 后端增加 `low_load -> comfort` 兼容映射并让 OpenAPI 公开正式五值 | 高优先级待开发 |
| C-07 | ETA 预测术语与数据口径 | 代码字段含 predicted/model_version，业务口径要求 LTA Bus Arrival 实时 ETA | 产品说明可能误称自研模型 | 文档称实时 ETA；评估重命名字段/服务类并保兼容 | 待确认 |
| C-08 | Load 预测术语与数据口径 | 路径 `/passenger-load-prediction`、字段 `predicted_load_*`，业务口径要求 LTA 实时客载 | 产品说明和接口名误导 | 设计正式实时 Load 路径，旧路径标兼容；核对实现是否确实取 LTA | 高优先级待确认 |
| C-09 | Passenger Flow 预测定位 | `/history/passenger-flow/prediction` 和写入接口仍运行 | 与第一阶段“历史分析”定位冲突 | 保留运行接口但页面暂不接入、不作为核心模型宣传 | 已在文档标注 |
| C-10 | 前端已封装但未调用 | 除独立 AI 页外，用户、地图、线路、车辆、历史、推荐等封装均无页面调用 | 旧文档误报接入率 | 统一状态改为“前端已封装未接入” | 已更正 |
| C-11 | 页面已调用但未验证 | `AiAssistantView` 调用 `/ai/travel`，但无路由入口和成功测试证据，catch 返回固定回答 | 容易把 Mock 成功当联调成功 | 注册路由、抓包、核对 answer/reminders/fallback | 待测试 |
| C-12 | 已废弃接口仍被前端暴露 | `refreshLtaBusArrival` 封装并由 index 导出，但未发现页面调用；后端 OpenAPI 标 deprecated | 后续可能误用 | 新增正式 `/admin/lta/*` 封装，旧方法加 deprecated 注释并逐步移除 | 待开发 |
| C-13 | 兼容函数名与实际路径不一致 | `getBusLines = getLines` 等函数实际请求 `/lines`，不请求 `/bus-lines` | 审计时易误判兼容接口已用 | 进度表按真实 URL 记录 | 已澄清 |
| C-14 | 旧文档遗漏现行接口 | OpenAPI 还包含 `/stations/nearby` GET、`/bus-lines/{id}/geometry`、`/location/*`、history 聚合兼容接口等 | 总览不完整 | 从 OpenAPI 自动更新接口总览，逐条标正式/兼容 | 总文档仍需后续细化表格 |
| C-15 | 错误/预留路径文案 | AI 页面显示 `/api/ai/chat`；PassengerFlow 页面显示 `/api/history/crowd` 等不存在路径 | 开发人员会按错误路径接入 | 改为当前正式路径或明确“未开发需求” | 待改页面文案（本次不改业务代码） |
| C-16 | 管理权限 | `/admin/lta/*` 和线路/站点/车辆写接口当前未绑定 admin 鉴权 | 管理页接入存在安全风险 | 正式接入前增加权限控制 | 待产品/后端确认 |
| C-17 | “页面是否存在”与“是否可访问” | 多个 Vue 页面存在，但 router 只注册 login/register/home/admin | 测试人员无法通过 UI 访问 | 先确认产品路由，再注册 AI/线路/车辆/客流页 | 待前端确认 |

## 必须先确认的决策

1. `comfort` 是立即修改 OpenAPI 的正式枚举，还是先以输入兼容别名方式上线；无论哪种方案，`low_load` 只能作为兼容值。
2. ETA/Load 是否新增语义准确的实时接口并保留旧路径，还是仅修正文档和内部实现命名。
3. `/history/passenger-flow/prediction` 是否长期保留为兼容/实验接口，还是进入废弃周期。
4. Vite 代理是否把 `/api` 重写到后端 `/api/v1`；若没有，当前默认配置不可用。
5. 哪些未注册页面属于本期范围，哪些只是代码草稿。
