# 接口文档冲突与待确认清单

> 核对日期：2026-07-12。优先级：运行时 OpenAPI > 后端代码 > 前端真实调用 > 历史文档/注释。

| 编号 | 冲突/待确认 | 代码与 OpenAPI事实 | 影响 | 建议 | 状态 |
|---|---|---|---|---|---|
| C-01 | 文档统计过期 | 实际 61 条路径、73 个操作（含 `/` 与 `/api/v1/` 两个健康检查 + 71 个业务操作） | 旧文档称 60 条路径/72 个操作 | 已更正为 61 条路径/73 个操作。 | 已确认 |
| C-02 | Base URL 不一致 | `request.js` 默认 `baseURL` 为 `/api/v1`（`VITE_API_BASE_URL \|\| '/api/v1'`） | 旧文档称默认 `/api` 与代码不符 | **已解决（文档端）**：`BusMind前端接口需求与页面接入说明.md` 统一说明已更正为 `/api/v1`；如需 `/api` 代理需配置 Vite proxy。 | 已确认（文档端） |
| C-03 | `page_size` 与 `limit` | 当前运行时列表/搜索分页为 `page`,`limit` | 旧调用会被忽略或 422 | **已解决**：后端全线使用 `page`+`limit`；前端 11 处调用均使用 `page`+`limit`（无 `page_size`）；`接口说明.md` 中 `page_size` 已更正为 `limit`。 | 已确认 |
| C-04 | `trace_id` 与 `request_id` | 后端所有响应统一使用 `trace_id`（`ApiResponse` 必填字段，7 个路由模块全部携带）；后端代码中 `request_id` 零出现；`request_id` 仅存在于推荐模型服务内部协议（`algorithm/model/contracts.py`、数据契约文档），非 BusMind API 统一外壳 | 前端未读取或展示 `trace_id`，错误排查时无法关联日志 | **已解决（后端+文档端）**：`接口说明.md` 示例中 `request_id` 已更正为 `trace_id`；后端所有响应已统一。前端尚未接入：无页面或工具函数读取 `trace_id`，接入说明已标注此状态。建议后续在错误提示中展示 `trace_id` 供用户反馈。 | 待前端接入 |
| C-05 | `predicted_eta_minutes` 与 `eta_minutes` | ETA Schema 与推荐结果均为 `predicted_eta_minutes`；组件 Mock 用 `eta_minutes`/`eta` | 页面适配失败；术语误导 | **已解决**：`src/utils/eta.js` 适配层统一提取（优先级 `predicted_eta_minutes` → `eta_minutes` → `eta` → `total_time_minutes`）；BusMap mock 已改为 `predicted_eta_minutes`；HomeView/MapSidebar 已通过 `formatEtaDisplay()` 展示。旧字段兼容保留。 | 已确认 |
| C-06 | `low_load` 与 `comfort` | OpenAPI `Preference` 曾是 `low_load`，无 `comfort`；代码未见映射 | 正式前端传 comfort 会 422 | 后端增加 `comfort` 正式枚举，`low_load` 作为兼容别名保留，`_weights_for_preference`/`_sort_by_preference` 同时匹配两者 | ✅ 已修复 |
| C-07 | ETA 预测术语与数据口径 | 代码字段含 predicted/model_version，业务口径要求 LTA Bus Arrival 实时 ETA | 产品说明可能误称自研模型 | **已解决**：代码注释、OpenAPI 描述、接口文档均已标注为 LTA 实时 ETA；字段 predicted_eta_minutes/model_version 为历史兼容命名，非 ML 预测。 | 已确认 |
| C-08 | Load 预测术语与数据口径 | 路径 `/passenger-load-prediction`、字段 `predicted_load_*`，业务口径要求 LTA 实时客载 | 产品说明和接口名误导 | 新增 `POST /realtime-passenger-load` 正式接口，响应字段去掉 predicted 前缀；旧路径保留兼容；数据源确认为 LTA 实时 + MySQL 缓存 | ✅ 已修复 |
| C-09 | Passenger Flow 预测定位 | `/history/passenger-flow/prediction` 保留运行，但**非第一阶段核心能力**，定位为兼容/实验接口 | 与第一阶段"历史分析"定位冲突 | 页面已接入但不作为核心宣传；展示标题为"近期客流趋势"；已在总览表、4.7节、4.7.6节、接口说明.md、接入说明、进度表统一标注 | 已确认 |
| C-10 | 前端已封装但未调用 | 除独立 AI 页外，用户、地图、线路、车辆、历史、推荐等封装均无页面调用 | 旧文档误报接入率 | 统一状态改为“前端已封装未接入” | 已更正 |
| C-11 | 页面已调用但未验证 | `AiAssistantView` 调用 `POST /api/v1/ai/travel`，路由已注册 `/ai` | 容易把 Mock 成功当联调成功 | **已联调验证**（2026-07-12）：QA 模式成功响应（code=0, fallback=true, answer/reminders 字段正常）；参数校验错误响应（code=42200, trace_id 正常）；suggest 模式需 MySQL 运行。前端正确读取 `answer`+`reminders`，但不读取 `fallback`/`used_tools`/`trace_id`。详见项目主文档 4.11.1 节联调记录。 | 已确认 |
| C-12 | 已废弃接口仍被前端暴露 | `refreshLtaBusArrival` 封装并由 index 导出，但未发现页面调用；后端 OpenAPI 标 deprecated | 后续可能误用 | **已解决**：`simulation.js` 全面标注 @deprecated 并移除 index.js 统一导出；新代码统一走 `@/api/admin` 的正式 `/admin/lta/*` 路径。 | 已确认 |
| C-13 | 兼容函数名与实际路径不一致 | `getBusLines = getLines` 等函数实际请求 `/lines`，不请求 `/bus-lines` | 审计时易误判兼容接口已用 | 进度表按真实 URL 记录 | 已澄清 |
| C-14 | 旧文档遗漏现行接口 | 原总览表遗漏 10 个接口：`GET /api/v1/`、`/bus-lines/{id}/map`、`/bus-lines/{id}/geometry`、`/bus-stations/nearby`、`/bus-vehicles/realtime`、`/stations/nearby`（GET/405）、`/history/passenger-load`、`/history/predictions`、`/location/nearby`、`/location/{station_id}` | 总览不完整 | **已解决**：10 个遗漏接口已全部补入总览表（序号 63–72），逐条标注正式/兼容/废弃 | 已确认 |
  | C-15 | 错误/过期接口文案 | AI 页面显示 `/api/ai/chat`；PassengerFlow 页面显示 `/api/history/crowd` 等不存在路径 | 开发人员会按错误路径接入 | **已解决**：AI 页面 `sider-note` 已显示正确路径 `POST /api/v1/ai/travel`（文档过时已更正）；PassengerFlow 页面标题/标签已统一为"历史分析优先"措辞，去掉"预测"误导；script 逻辑未改动。 | 已确认 |
| C-16 | 管理权限 | `/admin/lta/*` 和线路/站点/车辆写接口当前未绑定 admin 鉴权 | 管理页接入存在安全风险 | 为 `/admin/lta/*`、线路/站点/车辆 POST/PATCH/DELETE、仿真写操作添加 `get_current_admin_user` 依赖；非 admin 返回 403 | ✅ 已修复 |
| C-17 | "页面是否存在"与"是否可访问" | 所有 Vue 页面（AI/线路/车辆/客流/个人中心）均已注册路由，可通过 UI 访问 | 旧文档称"只注册 login/register/home/admin"已过期 | **已解决**：router 已注册全部 7 个页面（`/ai`、`/lines`、`/lines/:id`、`/vehicles`、`/passenger-flow`、`/profile`、`/home`）。`BusMind前端接口需求与页面接入说明.md` 页面总览表已同步更新。 | 已确认 |

## 必须先确认的决策

1. `comfort` 已作为正式枚举添加到 OpenAPI，`low_load` 作为兼容别名保留，两者行为一致。✅
2. ETA/Load 已新增语义准确的实时接口（`POST /realtime-passenger-load`），旧路径保留兼容。✅
3. `/history/passenger-flow/prediction` 保留为兼容/实验接口，不作为第一阶段核心能力。✅
4. 前端 `baseURL` 已确认为 `/api/v1`，无需 Vite proxy 重写。✅
5. 所有 7 个页面均已注册路由，可通过 UI 访问。✅
6. `POST /api/v1/ai/travel` QA 模式已联调通过（2026-07-12），`answer`/`reminders`/`fallback`/`trace_id` 字段均验证正常。✅