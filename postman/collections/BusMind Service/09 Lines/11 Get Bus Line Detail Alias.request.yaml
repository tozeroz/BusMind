$kind: http-request
url: "{{base_url}}/bus-lines/{{line_id}}"
method: GET
headers:
  Accept: application/json
scripts:
  - type: afterResponse
    code: |-
      pm.test('HTTP status is expected', function () { pm.expect(pm.response.code).to.be.oneOf([200, 404, 422]); });
    language: text/javascript
order: 11000
