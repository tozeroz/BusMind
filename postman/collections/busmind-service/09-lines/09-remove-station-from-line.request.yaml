$kind: http-request
url: "{{base_url}}/lines/stations/{{line_station_id}}"
method: DELETE
headers:
  Authorization: "Bearer {{access_token}}"
  Accept: application/json
scripts:
  - type: afterResponse
    code: |-
      pm.test('HTTP status is expected', function () { pm.expect(pm.response.code).to.be.oneOf([200, 401, 403, 404, 422]); });
    language: text/javascript
order: 9000
